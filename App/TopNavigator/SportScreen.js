import React from 'react';
import SingleSource from '../component/SingleSource';
const SportsScreen = ({navigation}) => {
  return <SingleSource nav={navigation} Source={'sports'} />;
};

export default SportsScreen;
