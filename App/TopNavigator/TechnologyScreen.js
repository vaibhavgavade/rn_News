import React, {useState, useEffect} from 'react';
import SingleSource from '../component/SingleSource';
const Technology = ({navigation}) => (
  <SingleSource nav={navigation} Source={'technology'} />
);

export default Technology;
